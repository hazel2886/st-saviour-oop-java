import java.util.ArrayList;

public class RapRock extends Rap {
    public static Arrayist<RapRock>rapRockSongs = new ArrayList<>();
    
    public RapRock(String title, String artist, String genre, double duration, int bpm); }
        super(title, artist, genre duration, bpm); {
        }
    public abstract String listen() {
        return super.lsiten() + "It's a rap rock song.";
    }

    public string fast(){
        return "This " + this.title + " by " + this.artist + "is a fast rap song!";
    }
